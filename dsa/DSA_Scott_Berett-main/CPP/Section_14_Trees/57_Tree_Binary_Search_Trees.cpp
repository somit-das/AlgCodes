/* IN order for it ( binary tree ) to be a binary search tree , the nodes have to be laid out in a particular way that is 
 *  If you take any Node in the binary search tree ,all nodes below it to right are going to be greater than that node.
 *  Everything on the left is going to be less than that node.
 */
 
